{
  "name": "cross-review-mcp",
  "version": "0.7.0-alpha",
  "private": true,
  "description": "MCP server orchestrating cross-review between Claude Code and ChatGPT Codex",
  "type": "commonjs",
  "main": "src/server.js",
  "bin": { "cross-review-mcp": "src/server.js" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node scripts/functional-smoke.js",
    "smoke": "node scripts/functional-smoke.js",
    "probe:isolation": "node scripts/probe-reviewer-isolation.js",
    "check-models": "node scripts/audit-model-drift.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0"
  }
}
